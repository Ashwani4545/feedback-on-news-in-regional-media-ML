version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
  streamlit:
    build: .
    command: streamlit run dashboard/streamlit_app.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    env_file: .env
